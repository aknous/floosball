# Floosball API - Python Dependencies
# Install with: pip install -r api_requirements.txt

# Core API Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# WebSocket support
websockets>=12.0

# Auth (Clerk JWT verification via JWKS)
PyJWT[crypto]>=2.8.0
python-multipart>=0.0.7

# SVG to PNG conversion (team avatar export)
cairosvg>=2.7.0

# Already required by main application
# (These should already be installed)
# sqlalchemy>=2.0.0
# numpy>=1.24.0
# matplotlib>=3.7.0
